/**
 * Shared runtime types for the extension ↔ webview contract.
 *
 * Data model schemas will be added in SPEC-04. For now this module only
 * re-exports the message types so both sides can depend on a single source.
 */
export * from './messages.js';
